version: "3.8"

services:
  privacypassdemo:
    # The image tag will be replaced by the GitHub Actions workflow
    image: ghcr.io/korjavin/privacypassdemo:704b4fd792f1ce2e164754f7456732eb461f3a43
    container_name: privacypassdemo
    ports:
      - "3000:3000"
    networks:
      - vaultwarden_default
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.privacypassdemo.rule=Host(`${DNS_NAME}`)"
      - "traefik.http.routers.privacypassdemo.entrypoints=websecure"
      - "traefik.http.routers.privacypassdemo.tls.certresolver=myresolver"
      - "traefik.http.services.privacypassdemo.loadbalancer.server.port=3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

networks:
  vaultwarden_default:
    external: true